---
description: Anti-hallucination. Source priority. Path truth.
alwaysApply: true
---

# Context

## Read order (before edit)
1. `docs/DECISIONS.md`
2. `docs/ARCHITECTURE.md`
3. `docs/TODO.md`
4. target files in `src/` (grep/read)

## Source priority (conflict)
`src/` > DECISIONS > ARCHITECTURE > TODO > LAB_PLAN > README

## Paths
- canonical Lab URL: `/lab/*`
- folder: `src/app/lab/`
- nav SSOT: `src/app/lab/_lib/routes.ts`
- MUST match: `routes.ts` href = `app/lab/` folder = hardcoded Links

Route change order: `routes.ts` → page → nav

## Existence
grep/read before import. Missing → say "not implemented", do not invent.

## Metrics
numbers only from `lib/data/portfolio-metrics.ts` or `StatsSection` (until metrics file exists)

## Git
commit/push ONLY on user request. pre-commit: lint-staged.

## Rule priority
`.cursor/rules` > user_rules (RN etc.)

## Session header (3 lines)
branch / phase / this change
